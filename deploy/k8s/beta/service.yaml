apiVersion: v1
kind: Service
metadata:
  name: llm-agent
  namespace: llm-agent
  labels:
    app.kubernetes.io/name: llm-agent
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: llm-agent
  ports:
    - name: http
      port: 80
      targetPort: http
      protocol: TCP
