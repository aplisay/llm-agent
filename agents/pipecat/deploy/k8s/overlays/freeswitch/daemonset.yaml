# Adds the FreeSWITCH SIP/media container + the esl-poller call-control sidecar
# to the per-node pod, and points the worker at the freeswitch gateway.
#
# FreeSWITCH's entrypoint has a real shell, so we wrap it directly to export the
# detected node IP into EXT_IP_ADDRESS (consumed by vars.xml `env-set`). The
# entrypoint also concatenates a mounted /certs/tls.{crt,key} into its TLS pem,
# falling back to its baked-in self-signed cert when the Secret is absent.
apiVersion: apps/v1
kind: DaemonSet
metadata:
  name: pipecat-sip
  namespace: pipecat
spec:
  template:
    spec:
      containers:
        - name: pipecat-worker
          env:
            - name: SIP_GATEWAY
              value: freeswitch

        - name: freeswitch
          # :next = staging default; components/env-production overrides to :latest.
          image: europe-west1-docker.pkg.dev/llm-voice/containers/llm-agent/freeswitch:next
          # secretenv-exec decrypts SECRETENV_BUNDLE into the env (FreeSWITCH
          # can't decrypt natively), then the shell wrapper adds the per-node IP
          # and execs FS's entrypoint. Harmless no-op when no bundle is set.
          command: ["/etc/node-meta/secretenv-exec", "/bin/sh", "-c"]
          args:
            - |
              export EXT_IP_ADDRESS="$(cat /etc/node-meta/ext-ip)"
              exec /usr/bin/entrypoint.sh
          envFrom:
            - configMapRef:
                name: pipecat-config
            - secretRef:
                name: pipecat-secrets
                optional: true
            - secretRef:
                name: pipecat-secretenv
                optional: true
          ports:
            - name: sip-udp
              containerPort: 5060
              protocol: UDP
            - name: sip-tls
              containerPort: 5061
              protocol: TCP
          livenessProbe:
            tcpSocket:
              port: 8021
            initialDelaySeconds: 20
            periodSeconds: 20
          volumeMounts:
            - name: node-meta
              mountPath: /etc/node-meta
              readOnly: true
            # FreeSWITCH entrypoint reads /certs/tls.crt + /certs/tls.key.
            - name: sip-tls
              mountPath: /certs
              readOnly: true
          resources:
            requests:
              cpu: 300m
              memory: 256Mi
            limits:
              memory: 1Gi

        - name: esl-poller
          # :next = staging default; components/env-production overrides to :latest.
          image: europe-west1-docker.pkg.dev/llm-voice/containers/llm-agent/esl-poller:next
          # esl-poller's dotenv (the secretenv fork) already decrypts
          # SECRETENV_KEY/BUNDLE natively at startup — no wrapper needed.
          envFrom:
            - configMapRef:
                name: pipecat-config
            - secretRef:
                name: pipecat-secrets
                optional: true
            - secretRef:
                name: pipecat-secretenv
                optional: true
          readinessProbe:
            tcpSocket:
              port: 4001
            initialDelaySeconds: 5
            periodSeconds: 10
          resources:
            requests:
              cpu: 100m
              memory: 64Mi
            limits:
              memory: 256Mi
